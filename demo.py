# demo.py


strA ="strA"
i = 100
j = 3.14
print(strA,i,j)

len("")

#list : 순서가 있고 임의 객체 집합 (입력, 수정, 삭제, 검색) // [~,~] 이런 구조를 띔
#Dict : 순서가 없고, 키를 사용해서 값을 꺼낸다. 맵핑구조 // {key : value} 이런 구조를 띔
#Tuple : 순서가 있고, 임의 객체의 집합 (수정이 안됨)  // (~,~) 구조
#Set : 순서가 없고, 집합 형태로 사용한다. 합집합, 교집합, 차집합 // {~,~} 구조


